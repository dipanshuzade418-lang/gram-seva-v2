# Copy this file to ".env.production" and fill in your deployed backend URL.
# This is used when you run `npm run build` for GitHub Pages / any static host.
#
# Example (after deploying the backend to Render):
# VITE_API_URL=https://gram-arogya-seva-api.onrender.com/api

VITE_API_URL=
